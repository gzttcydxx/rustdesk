name: Fork Probe Match

# Diagnostic only. Same as fork-probe-mismatch.yml but the push filter DOES
# match the branch it is pushed on, so this one is expected to run.
on:
  workflow_call:
  workflow_dispatch:
  push:
    branches:
      - "feat/no-login-address-book"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "probe-match"
